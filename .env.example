# PostgreSQL connection string for Prisma (optional)
# Leave empty to use fallback in-memory college data
# For production, use a managed PostgreSQL service like Supabase, AWS RDS, or Vercel Postgres
DATABASE_URL=
